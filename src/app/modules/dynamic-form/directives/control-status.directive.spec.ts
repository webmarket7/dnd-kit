import { ControlStatusDirective } from './control-status.directive';

describe('ControlStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlStatusDirective();
    expect(directive).toBeTruthy();
  });
});
