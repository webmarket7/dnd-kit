import { ActionMenuDirective } from './action-menu.directive';

describe('ActionMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionMenuDirective();
    expect(directive).toBeTruthy();
  });
});
