import { RenderListDirective } from './render-list.directive';

describe('RenderListDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderListDirective();
    expect(directive).toBeTruthy();
  });
});
